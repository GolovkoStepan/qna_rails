<% if @result %>
  $('#phone-confirmation-block').html("<%= j render partial: 'phone_confirmation/form' %>")
<% else %>
  $('#phone-confirmation-block').html('<div class="mt-2 mb-3"><i class="fas fa-times-circle"></i> Too many requests for sending codes. Only 5 requests per hour allowed.</div>')
<% end %>
